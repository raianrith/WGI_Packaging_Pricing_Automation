/** Synthetic package nav id for solutions with no package (standalone). */
export const STANDALONE_PACKAGE_NAV_ID = "__standalone__";

/** Synthetic solution nav id: show every solution and every tier in the sidebar. */
export const ALL_SOLUTIONS_NAV_ID = "__all_solutions__";
